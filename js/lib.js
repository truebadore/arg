/* lib.js - for ARG site specific jquery, js, etc */

$(document).ready(
  function() {
	  $('#site_search').click(
	    function($e) {
	      $('#search').slideToggle('fast');		
		}
		
      ); 
  }


);